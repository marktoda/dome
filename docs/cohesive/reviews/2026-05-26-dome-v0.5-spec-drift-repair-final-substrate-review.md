# Final Substrate Review — Dome v0.5 spec drift repair (Phase A pass-2)

**Verdict:** Pass

F1, F2, and F3 from the pass-1 substrate-alignment review are genuinely closed. The fixes wire the right seams, the tests pin the right behavior, and no new substrate-alignment issues appeared.

## What was verified

**F1 closed.** `src/vault.ts:213-215` registers `hookDispatcher.onCycleDetected` against `appendCycleLogEntry`, which emits via `privilegedWriter.appendLogEntry` in the exact `## [ts] hook.cycle-detected | handler=X depth=N` shape that `doctor.ts:304`'s regex parses. The wiring honors INDEX_AND_LOG_ARE_DISPATCHER_OWNED (only the privileged writer touches `log.md`). The test at `tests/hooks/cycle-log-writer.test.ts:42-79` covers both axes the pass-1 finding identified: (a) the formatter shape (lines 17-40) and (b) the dispatcher-driven listener path producing an entry doctor actually surfaces. The test acknowledges in-comment that the openVault-internal dispatcher is unreachable from outside and uses a parallel dispatcher with identical wiring — appropriate scope discipline given programmatic hook registration ships in v0.5.1+.

**F2 closed.** `src/vault.ts:273-276` composes `hookDispatcher.drain() + registry.flushPersist()` and the inline comment cites the exact `hooks.md` §"Execution model" Failure model claim it preserves. The new test at `tests/hooks/quarantine-persistence.test.ts:38-92` drives `recordFailure` through `dispatcher.dispatchEvents()` (not directly) and settles via `drain() + flushPersist()` only — never awaiting an individual save. This is the regression shape that would catch a future drainHooks regression.

**F3 closed.** `tests/hooks/quarantine-persistence.test.ts:94-145` replaces the round-trip with a behavioral test: load via `makeQuarantineStore.load()`, seed `HookRegistry` with `initialQuarantined`, then assert `matchesEvent("test.event")` filters out the preloaded handler. A contrast assertion confirms the filter is targeted (a healthy handler with the same pattern still matches). If `openVault` stopped calling `makeQuarantineStore(...).load()` or stopped passing `initialQuarantined`, this test fails.

## Note (informational, no action)

The `appendCycleLogEntry` export on `src/vault.ts` is fine where it sits — its formatter consumes a `CycleInfo` (from `hook-dispatcher.ts`) and writes via a `PrivilegedWriter`. Both deps live outside `hook-dispatcher.ts`, so moving the formatter into the dispatcher would invert the dependency direction. The current placement keeps the dispatcher pure (no log-write concerns) and matches the `appendLogEntry` neighbors already on `vault.ts`.

## Pass-1 findings still untouched (no new evidence they bite)

- **F4 (Low):** inbox-walk doesn't recurse — bucket flatness is implicit. No new evidence in pass-2.
- **F5 (Low):** `cli.md` flag list lacks shipped-vs-pending distinction. With F1 closed, the spec is now honest (all 9 flags genuinely functional); the validation-review I2 acceptance ("implementer reads the ledger") holds.

## Structure review pass-1 findings (carry-over notes)

The pass-1 structure reviewer flagged five points, all Medium or Low. None are blocking for Implemented:

1. **Doctor flag execution is six anonymous if-blocks (Medium).** Recommended deferral until a 10th flag arrives. Not closed in pass-2.
2. **Three independent log.md parsers in one file (Medium).** A `parseLogEntries` helper would collapse them. Not closed in pass-2; candidate for a small follow-up under a different slug.
3. **`recentActivityN: number | null` sentinel encoding (Low).** Cleaner as `recentActivityN?: number`. Not closed in pass-2.
4. **Dynamic `await import("../../quarantine-store")` in doctor.ts (Low).** Should be static. Not closed in pass-2.
5. **`quarantine-store.ts` seam placement validated (no action).**
6. **Matrix I1 closure clean (no action).**

These are appropriate to defer; none are substrate-alignment blockers.
