# End-of-run Coverage Review — Dome v0.5 spec drift repair (pass-2)

**Verdict:** Covered

All 14 non-Deferred ledger entries map to live diff hunks; the three follow-up commits closing F1/F2/F3 from pass-1's substrate-alignment review are in-scope machinery (not unmapped substrate work).

## Coverage table

| Entry ID | Plan task | Diff hunk(s) | Status |
|---|---|---|---|
| `shipped-config-inbox-stale-hours` | T1 | `src/vault.ts` VaultConfig, `src/shipped-defaults.ts` default 24 | Covered |
| `shipped-agents-md-offline-pointer` | T2 | `src/cli/commands/init.ts` Offline rule surface block; `tests/cli/init.test.ts` assertions | Covered |
| `doctor-drain-hooks` | T3 | `src/cli/commands/doctor.ts` drainHooks call; `tests/cli/doctor-flags.test.ts` | Covered — **strengthened in pass-2** (`vault.drainHooks` now also awaits `registry.flushPersist`) |
| `doctor-recent-activity` | T4 | `src/cli/commands/doctor.ts` log.md walk; `tests/cli/doctor-flags.test.ts` | Covered |
| `doctor-recent-activity-arg-parse` | T4 | `src/cli/cli.ts` `[N]` option + `toDoctorOpts` | Covered |
| `doctor-reset-quarantined` | T9 | `src/cli/commands/doctor.ts` clear branch | Covered |
| `doctor-show-review-queue` | T5 | `src/cli/commands/doctor.ts` review-queue walk | Covered |
| `doctor-show-raw-citations` | T6 | `src/cli/commands/doctor.ts` raw-citations walk | Covered |
| `doctor-show-recent-hook-cycles` | T7 | `src/cli/commands/doctor.ts` log.md parser | Covered — **strengthened in pass-2** (producer now exists via `appendCycleLogEntry` + `hookDispatcher.onCycleDetected` wiring at `src/vault.ts:207-213`; pass-1 parser stood against a fixture only) |
| `doctor-inbox-stale-check` | T10 | `src/cli/commands/doctor.ts` CHECK 9; `tests/cli/doctor-flags.test.ts` | Covered |
| `hookregistry-quarantine-persist-write` | T8 | `src/hook-registry.ts` persistPath / scheduleSave; `tests/hooks/quarantine-persistence.test.ts` | Covered — **strengthened in pass-2** (F2-closing test drives via dispatcher) |
| `hookregistry-quarantine-persist-read` | T8 | `src/vault.ts` load + seed; `tests/hooks/quarantine-persistence.test.ts` | Covered — **strengthened in pass-2** (F3-closing behavioral test asserts `matchesEvent` filters preloaded id) |
| `hookregistry-quarantine-persist-clear` | T9 | `src/cli/commands/doctor.ts` + `src/quarantine-store.ts` `clear()` | Covered |
| `inbox-ephemeral-doctor-test` | T11 | `tests/invariants/inbox-is-ephemeral.test.ts` dome doctor fallback describe block | Covered |

## Follow-up commit triage (F1/F2/F3 from pass-1 substrate review)

- **F1 (d219c5a) — cycle-listener wiring + `appendCycleLogEntry`.** Not a new ledger entry. The ledger's `doctor-show-recent-hook-cycles` is now genuinely functional (parser + producer) rather than parser-against-fixture; the implementation-handoff section says "`dome doctor` implements all 9 documented flags" — the producer is in-scope machinery for that bullet, and `hooks.md` §"Cycle prevention" already names cycle events as logged.
- **F2 (5acf32f) — `drainHooks` awaits `flushPersist`.** Not a new ledger entry. The "quarantine survives across processes" contract; `drainHooks` closing on the persistence chain is the integration glue.
- **F3 (8c6a20e) — behavioral quarantine-load test.** Replaces a vacuous round-trip assertion. Strengthens `hookregistry-quarantine-persist-read` coverage; doesn't add new substrate.

`tests/hooks/cycle-log-writer.test.ts` is new but ratifies machinery for `doctor-show-recent-hook-cycles`. The `appendCycleLogEntry` export is a small internal seam, not a new public surface.
