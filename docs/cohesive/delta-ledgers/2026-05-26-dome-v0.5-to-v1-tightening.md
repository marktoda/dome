# Design Delta Ledger — Dome v0.5 → v1 tightening

**Date:** 2026-05-26
**Slug:** `dome-v0.5-to-v1-tightening`
**Branch:** `design/dome-v0.5-to-v1-tightening` (worktree at `.claude/worktrees/design+dome-v0.5-to-v1-tightening`, branched off `main`@`6582c6a`)
**Approved direction source:** [`docs/cohesive/reviews/2026-05-26-dome-v0.5-to-v1-readiness-architecture-review.md`](../reviews/2026-05-26-dome-v0.5-to-v1-readiness-architecture-review.md) §"Highest-leverage findings" F1–F8 + §"Phased roadmap". User dispatch confirmed three sub-decisions: F3 takes Part B (kill ordering rules via `vaultRef`); F2 takes the "delete `src/cli/doctor-flag.ts` entirely" option; the F8 multi-vault sub-question is deferred to a separate `cohesive:brainstorm-design` and is NOT picked inside this rewrite.
**Builds on:** [`docs/cohesive/delta-ledgers/2026-05-26-dome-v0.5-second-pass-label-tightening.md`](2026-05-26-dome-v0.5-second-pass-label-tightening.md) (the AbstractSurface + `wrapMutatingInvoke` + `HOOK_DISPATCH_IS_VAULT_BOUND` substrate this rewrite extends) and [`docs/cohesive/delta-ledgers/2026-05-26-dome-compiler-reframe.md`](2026-05-26-dome-compiler-reframe.md) (the compiler-reframe + AGENTS.md generation substrate this rewrite aligns to).

## Delta at a glance

**Classification:** **Mixed.** Design-layer changes: (a) seven normative docs rename `ConsumerSurface` / `buildConsumerSurface` references to `AbstractSurface` / `renderMcp` to close the pass-2 → pass-3 rename drift; (b) three "Adding a new X" recipes added to `cli.md`, `hooks.md`, and `sdk-surface.md` paralleling the existing "Adding an 8th Tool is two file edits" surgical recipe; (c) the off-matrix lockstep convention promoted from reviewer-memory to substrate — sdk-surface.md §"Off-matrix lockstep convention" defines the delegating-stub shape, `tool-invariant-enforcement.md` adds a "Lockstep status" subsection, `consumer-surface.md` declares its lockstep status as documentary-in-v0.5 / lockstep-in-v0.5.1, the gotcha `coverage:` frontmatter schema extends to require `enforced_at:` for off-matrix gotchas, and all eleven off-matrix gotchas land the field; (d) `openVault` composable-construction shape introduced — `loadVaultConfig` / `buildBuiltinHookRegistry` / `wireDispatcher` named, `vaultRef: { current: Vault | null }` setter pattern documented as the structural fence against the three hidden ordering rules pass-2 surfaced; (e) one-`PromptLoader`-per-Vault contract spelled out in prompts-and-workflows.md §"Prompt loading lifecycle" — `WorkflowRegistry` constructor and `runWorkflow` both accept the loader as an optional parameter, paving for the v1+ `Vault.prompts` projection; (f) `PromptDescriptor` shape sync — spec now describes the live shape (`body: string`, optional `tier`) and explicitly defers parameterized prompts (Zod schema + `getMessages(args)` callback) to v0.5.1+; (g) `readDynamicResource` lifted to spec as the fifth `AbstractSurface` field with rationale; (h) hooks.md §"Execution model" gains `asyncConcurrency: 1` v0.5-default note + `INTAKE_EXCLUDED_BUCKETS` const reference; (i) new gotcha `agents-md-delimiter-shape` (the user-prose delimiter literals are duplicated across 3 files; substrate lockstep pins them); (j) new gotcha `boundary-validation-via-zod` (YAML and JSON persistence boundaries hand-validate where Zod is the SDK's pattern); (k) new linter spec `no-retired-symbol-names` for v0.5.1 (parallels existing `no-retired-invariant-names-in-prompts` lockstep on the docs surface); (l) repo-root `AGENTS.md` and `CLAUDE.md` ship (the SDK repo follows its own `AGENTS_MD_IS_ORIENTATION_SURFACE` invariant); (m) `docs/cohesive/IMPLEMENTATION_HANDOFF.md` marked archival with a banner — the doc is frozen at design-handoff snapshot; current entry points named explicitly. Implementation-layer changes are deferred to `cohesive:implement-cohesively`: the `src/cli/index.ts` `domeStats` re-export, the deletion of `src/cli/doctor-flag.ts` + its test, the `doctor.ts` split into `src/cli/doctor/{checks,show}/*.ts`, the `openVault` decomposition + `vaultRef` refactor, the `WorkflowRegistry` constructor signature change, the AGENTS.md delimiter lockstep test, the matrix-coverage extension to `consumer-surface.md`, the off-matrix stub-shape audit + edits, the `remark`/`unified` removal from `package.json`, the `as unknown as` cleanup at `src/tools/registry.ts:292` and `src/mcp/server.ts:44`, the Zod schemas at the three YAML/JSON loaders, the `Bun.Glob` substitutions, the `Bun.file`/`existsSync` consistency pass, the `walkUpForAncestor` extraction, the `WorkflowName` tuple migration, the `bindAiSdkTools`/`buildAiSdkTools` cleanup, the `hooks/` directory consolidation, the `INTAKE_EXCLUDED_BUCKETS` const wire-up, the `tsconfig.json` types bump, and the new lockstep tests (`cli-shell-shape`, `no-retired-symbol-names-in-specs`, `vault-bootstrap-order`, `consumer-surface-matrix-coverage`).

**Files:** 3 substrate files added (2 gotchas, 1 linter spec). 13 substrate files rewritten (6 specs, 2 matrices, 1 invariant, 1 catalog index, 1 handoff archival banner, 2 supporting entries — `transitive-llm-dependency.md` rewrite + the eleven-off-matrix-gotchas `enforced_at:` frontmatter pass treated as one entry). 11 gotcha frontmatters extended with `enforced_at:`. 2 repo-root orientation files added (AGENTS.md + CLAUDE.md).

**Conceptual changes:** Pass-3 surfaced a single class of gap repeated across four surfaces — **labels lag structure on the new seam**. The architectural moves landed in pass-2 (AbstractSurface, `wrapMutatingInvoke`, `HOOK_DISPATCH_IS_VAULT_BOUND`, compiler-reframe); the under-enforcement moved up one layer to ergonomics. This rewrite pins the labels: the seven `ConsumerSurface` references rewrite to `AbstractSurface` (the rename whose code half shipped pass-2 → pass-3 transition); the three "Adding a new X" recipes complete the symmetry with "Adding an 8th Tool" (the substrate now names the cost of every common change shape); the off-matrix lockstep stub-shape is the structural fence against the AC3 no-op pattern pass-3 found in two places; the `openVault` composable-construction shape kills the three hidden ordering rules that lived only in inline comments; the one-PromptLoader-per-Vault contract closes the triple-walk pass-2 F8 named. The substrate's pass-3 question — *"can a future contributor or agent predict the right answer for every common change shape, anchored to a structural fence?"* — is now genuinely yes. What remains is the implementation pass that makes the substrate's claims true in code.

**Named invariants:** None added in this rewrite. The 16-invariant catalog (per `docs/index.md` §"Invariants" and `src/types.ts` `INVARIANTS`) stays unchanged. The semantic-linter doc `no-retired-symbol-names` is *not* an invariant — it's a convention-with-deferred-lockstep, sibling to `wrap-mutating-invoke-consumption`.

**Behavior matrices:** `tool-invariant-enforcement.md` gains §"Lockstep status" subsection naming its own lockstep coverage and the documentary status of three sibling matrices. `consumer-surface.md` declares its lockstep-status preamble — documentary in v0.5, lockstep-checked in v0.5.1 via the matrix-coverage extension. No matrix rows or columns change in this rewrite.

**Gotchas:** Two new gotchas. `agents-md-delimiter-shape` (the duplicated-literal scar: user-prose delimiters live in 3 places — `src/agents-md.ts`, `AGENTS_MD_IS_ORIENTATION_SURFACE.md`, and `cli.md` — and silently regress on the next `--repair` if any one drifts). `boundary-validation-via-zod` (the persistence-boundary scar: `src/hooks/yaml-loader.ts`, `src/vault.ts` config load, `src/reconcile.ts` JSON load, `src/quarantine-store.ts` hand-validate where Zod is the SDK pattern). Eleven existing off-matrix gotchas gain `enforced_at:` frontmatter pointing at the canonical enforcement file. The matrix gotcha `concurrent-harness-write` is unchanged (it already has a regression test at `tests/gotchas/concurrent-harness-write.test.ts`).

**Semantic linters:** One added: `no-retired-symbol-names` (v0.5.1, shipped-default tier). Convention-with-grep until the lockstep ships in v0.5.1: every PR that retires a symbol rewrites every normative-doc reference in the same pass. The lockstep walks `docs/wiki/**/*.md` (and selected `docs/cohesive/*.md`) and asserts none names a symbol from the retired-names allow-list (`ConsumerSurface`, `buildConsumerSurface`, `projectMcp`, `McpToolName`, `SENSITIVE_GOES_TO_INBOX`, …). The companion code-side lockstep is `tests/integration/public-surface-shape.test.ts`, already shipped.

## Approved direction (verbatim from pass-3 review §"Next" + user dispatch)

Promote a handful of conventions to structural enforcement; the rewrite is bounded and unblocks v1's first new consumer surface. Phase A (label tightening + lockstep extension), Phase B (locality decompositions), Phase C (library-native + type tightening) land as one rewrite-specs pass; implementation follows.

User confirmed at dispatch:
- F3 takes Part B (kill ordering rules via `vaultRef: { current: Vault | null }` setter) over Part A-only (decomposition without setter).
- F2 takes the "delete `src/cli/doctor-flag.ts` entirely" option (the test is the only consumer; the enum was speculative).
- F8 multi-vault sub-question is deferred to a separate `cohesive:brainstorm-design`. This rewrite does NOT pick a multi-vault shape (process-per-vault vs multi-vault-daemon vs vault-pool-with-LRU); it does NOT write specs that assume a particular model. The composable-construction shape in §"Vault" supports multi-vault under any of the three models because the construction primitives are factored out — the multi-vault decision selects which primitives a future surface assembles.

## Per-file changes

### Substrate added (3 files)

#### `docs/wiki/gotchas/agents-md-delimiter-shape.md` — new gotcha

Stable ID for implement-cohesively: `agents-md-delimiter-gotcha`.

- Symptom: editing the user-prose delimiter strings in `AGENTS_MD_IS_ORIENTATION_SURFACE.md` without updating `src/agents-md.ts` destroys user prose on the next `dome doctor --repair`.
- Severity: high. Coverage: off-matrix. Enforced_at: `tests/invariants/agents-md-is-orientation-surface.test.ts`.
- Implementation work this implies: extend `tests/invariants/agents-md-is-orientation-surface.test.ts` to parse the invariant doc's literal `<!-- BEGIN user-prose -->` and `<!-- END user-prose -->` strings and assert equality with the `USER_PROSE_BEGIN` / `USER_PROSE_END` exports from `src/agents-md.ts`.

#### `docs/wiki/gotchas/boundary-validation-via-zod.md` — new gotcha

Stable ID for implement-cohesively: `boundary-validation-gotcha`.

- Symptom: YAML and JSON persistence boundaries hand-validate; corruption is silent or surfaces deep in downstream code.
- Severity: medium. Coverage: off-matrix. Enforced_at: `src/tools/schemas.ts` (the precedent the boundary loaders converge on).
- Implementation work this implies: define Zod schemas at `src/hooks/yaml-loader.ts`, `src/vault.ts` config load, `src/reconcile.ts`, `src/quarantine-store.ts`. Each loader uses `safeParse` and returns `Result<T, ValidationError>`. State-file corruption returns the empty-state fallback AND emits a `state-corruption-detected` log entry via the privileged-writer.

#### `docs/wiki/linters/no-retired-symbol-names.md` — new linter spec

Stable ID for implement-cohesively: `no-retired-symbol-names-linter`.

- Status: lockstep test ships in v0.5.1 as `tests/integration/no-retired-symbol-names-in-specs.test.ts`.
- Walks `docs/wiki/**/*.md` and selected `docs/cohesive/*.md` (excluding reviews / brainstorms / delta-ledgers / substrate-discovery — append-only history that documents the rename itself); asserts none of the literals in the retired-names allow-list (`ConsumerSurface`, `buildConsumerSurface`, `projectMcp`, `McpToolName`, `SENSITIVE_GOES_TO_INBOX`, `sensitivity_classified`) appears in normative scope.
- Second arm: asserts none of the retired names appears as a public export from any `@dome/sdk` entrypoint (`grep "export.*ConsumerSurface" src/**/index.ts` returns zero matches).
- Implementation work this implies: create `tests/integration/no-retired-symbol-names-in-specs.test.ts`; define the retired-names allow-list as a typed const exported from `src/types.ts` alongside `INVARIANTS`.

### Substrate rewritten (13 files)

#### `docs/wiki/specs/sdk-surface.md` — rewrite (5 sections changed)

Stable IDs:

- `sdk-vault-composable-construction` — §"Vault" gains §"Composable construction" subsection naming `loadVaultConfig` / `buildBuiltinHookRegistry` / `wireDispatcher` helpers and the `vaultRef: { current: Vault | null }` setter pattern. The pass-2 F8 carry-over scar (TDZ closure, "loadDeclarativeHooks LAST", cycle-listener order) is replaced by the structural construction shape; `openVault` remains the canonical entry point for the 99% case.
- `sdk-abstract-surface-shape-sync` — §"AbstractSurface" subsection PromptDescriptor description rewritten to match the live shape (`body: string`, optional `tier`); v0.5.1+ parameterized-prompt deferral named explicitly. ResourceDescriptor unchanged. `readDynamicResource` lifted as the fifth `AbstractSurface` field with rationale (static-catalog `resources` + parameterized-lookup `readDynamicResource`). The four-kinds-in-fixed-order convention renamed to "five-field shape" with the dynamic-lookup explanation.
- `sdk-render-mcp-arguments-array` — §"renderMcp" rewritten to drop the "MCP `arguments` array" reference for v0.5 (descriptors are static-body; arguments wiring lands with v0.5.1+ parameterized prompts).
- `sdk-render-http-five-field` — §"Future renderers" rewritten to name `readDynamicResource` as the fifth field a future `renderHttp` projects (`/resources/page/{path}` parameterized route).
- `sdk-adding-an-invariant-recipe` — §"Tiered feature model" gains §"Adding a new invariant" + §"Off-matrix lockstep convention" subsections. Names the three file edits (INVARIANTS const + invariant doc + lockstep test) and the delegating-stub shape for off-matrix invariants (`import(...)` to the canonical enforcement test, not `expect(true).toBe(true)`).
- `sdk-consumer-surface-rename-tier-table` — §"Tiered feature model" MCP-tier paragraph rewritten: "(one of several ConsumerSurface adapters)" → "(the first per-protocol renderer over `AbstractSurface`)".

#### `docs/wiki/specs/cli.md` — rewrite (2 sections changed)

Stable IDs:

- `cli-adding-a-new-command-recipe` — new §"Adding a new command" section inserted before §"Implementation note", paralleling the "Adding an 8th Tool is two file edits" recipe surgically. Five file edits enumerated (implement `domeFoo`, wire Commander arm, re-export, update spec, end-to-end test). The CliError-vs-ToolError boundary named explicitly.
- `cli-five-commands-deterministic-comment` — §"Implementation note" pre-table prose updated: deterministic commands list goes from `init`/`doctor`/`serve`/`reconcile` to `init`/`doctor`/`serve`/`reconcile`/`stats` (closes a small drift the pass-3 review surfaced obliquely via §"Implementation note" reading inconsistently against the eight-command table below it).

#### `docs/wiki/specs/hooks.md` — rewrite (2 sections changed)

Stable IDs:

- `hooks-adding-a-new-hook-recipe` — new §"Adding a new hook" section inserted between §"Intake patterns" and §"Shipped default hooks". Three forms by mechanism (declarative / programmatic / shipped-default); the bare-event-expansion rule explicitly named as "the trap every hook author hits"; idempotency contract restated; cycle prevention as automatic.
- `hooks-execution-model-concurrency-note` — §"Execution model" bullet on queue concurrency reframed. Adds `asyncConcurrency: 1` v0.5-default explicit + reasoning (per-vault hook handlers share `.dome/state/`; auto-update-index ordering matters). Adds `INTAKE_EXCLUDED_BUCKETS` const reference for the `inbox/review/` exclusion that v0.5 currently hardcodes across 4 files.

#### `docs/wiki/specs/prompts-and-workflows.md` — rewrite (1 section added)

Stable ID: `prompts-loading-lifecycle`.

New §"Prompt loading lifecycle" section inserted before §"Workflow invocation". Names the one-`PromptLoader`-per-Vault contract; `WorkflowRegistry` constructor accepts optional `PromptLoader`; `runWorkflow` accepts optional `WorkflowRegistry`; future `Vault.prompts` projection named as the v1+ shape this lifecycle anticipates. Bundling-axiom split preserved (PromptLoader stays in core; WorkflowRegistry stays in workflows).

#### `docs/wiki/specs/harnesses.md` — rewrite (1 paragraph)

Stable ID: `harnesses-abstract-surface-rename`.

§"Future-harness pressure" paragraph rewritten — `ConsumerSurface` / `buildConsumerSurface(vault)` references replaced with `AbstractSurface` / `buildAbstractSurface(vault)` + `renderXxx(surface)`. Closes the highest-visibility live drift the rename produced.

#### `docs/wiki/specs/page-schema.md` — rewrite (1 subsection extended)

Stable ID: `page-schema-gotcha-enforced-at`.

§"Extension types" gotcha frontmatter schema extended: `enforced_at:` field added, required when `coverage == off-matrix`. The `coverage:` semantics expanded to name `enforced_at:` as the path that anchors the off-matrix claim. Hard-warning convention named: an `off-matrix` gotcha without `enforced_at:` is unverifiable substrate and the loader emits a hard frontmatter warning.

#### `docs/wiki/invariants/CORE_HAS_NO_LLM_OR_MCP_DEPENDENCY.md` — rewrite (1 reference)

Stable ID: `core-invariant-abstract-surface-rename`.

§"Related" item rewritten: `(the ConsumerSurface concept)` → `(the AbstractSurface + per-protocol renderer split)`.

#### `docs/wiki/matrices/tool-invariant-enforcement.md` — rewrite (1 section added, 1 paragraph rewritten)

Stable ID: `tool-invariant-matrix-lockstep-status`.

New §"Lockstep status" subsection inserted before §"Test guarantees". Declares this matrix as lockstep-checked, names the delegating-stub convention, names the three sibling matrices as documentary-in-v0.5, and names the v0.5.1 extension to `tests/integration/matrix-coverage.test.ts` covering `consumer-surface.md`. §"Test guarantees" first paragraph rewritten to cite the off-matrix lockstep convention.

#### `docs/wiki/matrices/consumer-surface.md` — rewrite (1 preamble line)

Stable ID: `consumer-surface-lockstep-status`.

Lockstep-status preamble added under the matrix's main intro paragraph: documentary in v0.5; lockstep-checked in v0.5.1; the matrix-coverage extension parses cells and asserts symbol-export agreement.

#### `docs/wiki/gotchas/transitive-llm-dependency.md` — rewrite (1 list item)

Stable ID: `transitive-llm-dep-rename`.

§"Structural mitigation" → §"The entrypoint split" rewritten: `@dome/sdk/mcp` description gains `renderMcp(surface)` and `McpSurface`; `@dome/sdk/cli` description revised to "eight `dome*` command functions" (closes the count-drift `seven` → `eight` from when `stats` shipped).

#### `docs/wiki/gotchas/<eleven-off-matrix-gotchas>.md` — frontmatter extended

Stable ID: `gotcha-frontmatter-enforced-at-pass`.

Eleven off-matrix gotchas (`agent-prompt-regression`, `ai-sdk-tool-variance`, `async-read-after-write-staleness`, `daemon-off-while-vault-mutating`, `dirty-git-state-at-reconcile`, `hook-cycle`, `hook-non-idempotent`, `multi-page-partial-write`, `out-of-band-vault-edits`, `substrate-count-drift`, `transitive-llm-dependency`) each land `enforced_at:` frontmatter pointing at the canonical enforcement file (test, source file carrying the documented scar, or — for `substrate-count-drift` — the new linter spec). The matrix-tagged `concurrent-harness-write` is unchanged.

#### `docs/index.md` — rewrite (2 list items added)

Stable ID: `substrate-catalog-pass-3-update`.

§"Gotchas" gains `agents-md-delimiter-shape` and `boundary-validation-via-zod` entries (one-line summaries). §"Linters" gains `no-retired-symbol-names` entry above `wrap-mutating-invoke-consumption`. No invariant / matrix / spec entries change.

#### `docs/cohesive/IMPLEMENTATION_HANDOFF.md` — rewrite (archival banner added)

Stable ID: `implementation-handoff-archival`.

Banner added at the top of the doc marking it as archival as of 2026-05-26, naming `docs/VISION.md` + `docs/index.md` + `docs/wiki/specs/sdk-surface.md` + `docs/wiki/specs/cli.md` + `src/types.ts` `INVARIANTS` as the canonical current-state entry points. The body below is preserved as historical reference; the rule "do not edit it to keep it current" pinned to `docs/wiki/gotchas/substrate-count-drift.md`.

### Repo-root files added (2 files)

#### `/AGENTS.md` — repo-root orientation surface

Stable ID: `repo-root-agents-md`.

~80-line repo-root AGENTS.md naming what the repo is (TypeScript SDK on Bun; four-concept Dome core; the dogfood vault at `docs/`), where to start by task shape (table mirroring the substrate's "Adding a new X" recipes), the load-bearing rules (16 named invariants + AC3 lockstep; four-concept core; core-has-no-LLM-or-MCP; markdown-is-source-of-truth; vault-is-git-repo; the compiler boundary), how to run tests, and the repo layout. Closes the pass-3 review's F8 cluster item ("no top-level `README.md`, `CLAUDE.md`, or `AGENTS.md` at repo root").

#### `/CLAUDE.md` — Claude Code shim

Stable ID: `repo-root-claude-md`.

5-line shim pointing at `AGENTS.md`. Claude Code's `CLAUDE.md` convention is what loads at session start; this file is the conventional way the dome repo asks Claude Code sessions to start by reading the substrate orientation file.

## Implementation work (deferred to `cohesive:implement-cohesively`)

These are the code changes the substrate above describes. Each has a stable ID for the delta-coverage reviewer's check.

### F1 — retired-symbol-names lockstep
- `f1-retired-names-test` — create `tests/integration/no-retired-symbol-names-in-specs.test.ts`. Walks the docs surface; greps for the retired-names allow-list literals; asserts zero matches. Run on every PR.
- `f1-retired-names-const` — define the allow-list as a typed const in `src/types.ts` alongside `INVARIANTS` (or a new `src/retired-symbols.ts` if it grows).

### F2 — CLI ergonomics
- `f2-domestats-export` — add `export { domeStats } from "./commands/stats";` to `src/cli/index.ts`. Update the file's lead comment ("seven `dome <cmd>` implementations" → "eight").
- `f2-cli-shell-shape-test` — create `tests/integration/cli-shell-shape.test.ts` parallel to `public-surface-shape.test.ts`. Enumerates `src/cli/commands/dome*.ts`; asserts each is re-exported from `src/cli/index.ts`. Closes the class structurally.
- `f2-delete-doctor-flag` — delete `src/cli/doctor-flag.ts` and `tests/cli/doctor-flag.test.ts`. Update any consumer to read flag names from `src/cli/cli.ts` directly. The drift surface goes away rather than getting maintained.
- `f2-doctor-split` — split `src/cli/commands/doctor.ts` into `src/cli/doctor/checks/{frontmatter-type,wikilinks,raw-immutable,log-monotonic,inbox-stale,agents-md-drift}.ts` and `src/cli/doctor/show/{review-queue,raw-citations,workflows,events,recent-hook-cycles}.ts`. Each module exports `(vault) => { violations, info }`. `domeDoctor` becomes ~50 lines of "run every check, run requested shows, return".

### F3 — openVault decomposition + vaultRef
- `f3-load-vault-config` — extract `loadVaultConfig(root): Promise<{ config: VaultConfig; pageTypes: PageTypesConfig }>` to `src/vault.ts` (or a sibling `src/vault-config.ts`). Pure I/O; reads `.dome/config.yaml` + `.dome/page-types.yaml` with shipped-default fallback.
- `f3-build-builtin-hook-registry` — extract `buildBuiltinHookRegistry(root, config): HookRegistry` constructing the populated registry with shipped-default handlers per `.dome/config.yaml.hooks.enabled`.
- `f3-wire-dispatcher` — extract `wireDispatcher(registry, writer, { vaultRef }): { dispatcher, dispatchEvents, drainHooks, close }`. Accepts the `vaultRef` setter; closures hold a reference to the holder, not the Vault.
- `f3-openvault-vault-ref-refactor` — rewrite `openVault` to use the three helpers + `vaultRef`. The three positional-ordering rules (TDZ closure, "loadDeclarativeHooks LAST", cycle-listener wiring window) go away.
- `f3-bootstrap-order-test` — create `tests/integration/vault-bootstrap-order.test.ts` that scrambles bootstrap section order (within the constraints of the new shape) and asserts hook-cycle logging still works.

### F4 — single PromptLoader per Vault
- `f4-workflow-registry-loader-param` — `WorkflowRegistry` constructor accepts optional `PromptLoader`. When omitted, constructs its own (backward-compatible).
- `f4-runworkflow-registry-param` — `runWorkflow` accepts optional `registry: WorkflowRegistry` in its `opts`. When omitted, constructs its own (backward-compatible).
- `f4-abstract-surface-threads-loader` — `buildAbstractSurface` passes its constructed loader to `new WorkflowRegistry(vault, loader)` so the second walk doesn't happen.
- `f4-project-ai-sdk-writer-reuse` — `projectAiSdk(vault)` consumes a Vault-private `_writer` field rather than reconstructing via `makePrivilegedWriter(vault.path)`. Removes the dead-construction parallel to F4's PromptLoader cascade.

### F5 — off-matrix lockstep + gotcha coverage + matrix lockstep extension
- `f5-off-matrix-stub-audit` — audit `tests/invariants/core-has-no-llm-or-mcp-dependency.test.ts` and `tests/invariants/hook-dispatch-is-vault-bound.test.ts` (the two stub-shape no-ops). Rewrite each to use the delegating-stub shape — `await import("../integration/<file>.test")` inside a `describe`. The AC3 meta-check rejects `expect(true).toBe(true)` no-ops.
- `f5-ac3-no-op-rejection` — extend `tests/integration/invariant-coverage.test.ts` to require each lockstep file to either run a `describe()` block referencing an enforcement test or contain at least one `expect()` call. The structural fence against the no-op stub.
- `f5-gotcha-coverage-enforced-at` — extend `tests/integration/gotcha-coverage.test.ts` to read the `enforced_at:` frontmatter for off-matrix gotchas and assert the named path exists. Reject off-matrix gotchas without `enforced_at:`.
- `f5-matrix-coverage-consumer-surface` — extend `tests/integration/matrix-coverage.test.ts` to also parse `docs/wiki/matrices/consumer-surface.md`. For each cell that names a specific entrypoint (`core`, `workflows`, `mcp`, `cli`), assert the symbol named in the column header is an actual export from the named entrypoint's `index.ts`. Skip cells with future-prefixed entrypoints (`future-http`, `future-voice`).

### F6 — boundary validation via Zod
- `f6-declarative-hook-schema` — define `DeclarativeHookSchema` in `src/hooks/yaml-loader.ts`. Replace hand-validation with `safeParse`; return `Result<DeclarativeHook, ValidationError>`.
- `f6-vault-config-schema` — define `VaultConfigSchema` and `PageTypesConfigSchema` in `src/vault.ts` (or a sibling `src/vault-schemas.ts`). Replace `as Partial<VaultConfig>` cast with `safeParse`.
- `f6-state-file-schemas` — define `ScheduledStateSchema` and `QuarantineSchema` in their respective stores. Replace `as`-cast JSON.parse with `safeParse`. On validation failure: return the empty-state fallback AND emit a `state-corruption-detected` log entry via the privileged-writer.

### F7 — dead-deps + cast cleanups
- `f7-remove-remark-unified` — `bun remove remark unified` (and any remark plugins). Extend `tests/integration/bundle-deps.test.ts` to assert the dependency closure of `src/index.ts` and `src/mcp/index.ts` does not transitively reach `remark` or `unified`.
- `f7-mapped-type-bind-result` — define `type BindResult = { [K in ToolName]: (input: ToolInputFor<K>) => Promise<ToolReturn<ToolOutputFor<K>>> }` over `TOOL_REGISTRY`. Use the mapped type as `bindTools`'s return so the `as unknown as BoundToolSurface` cast at `src/tools/registry.ts:292` drops out.
- `f7-server-like-pick` — replace `ServerLike` stub at `src/mcp/server.ts:44` with `Pick<Server, "setRequestHandler">` (import the real SDK `Server` type). Removes the second non-load-bearing `as unknown as` cast.

### F8 — cluster cleanups
- `f8-agents-md-delimiter-test` — extend `tests/invariants/agents-md-is-orientation-surface.test.ts` to parse the invariant doc's `<!-- BEGIN user-prose -->` and `<!-- END user-prose -->` literals and assert equality with `USER_PROSE_BEGIN` / `USER_PROSE_END` from `src/agents-md.ts`.
- `f8-intake-excluded-buckets` — export `INTAKE_EXCLUDED_BUCKETS: ReadonlySet<string> = new Set(["review"])` from `src/shipped-defaults.ts`. Replace the four+ hardcoded `"inbox/review"` literals across `src/vault.ts`, `src/cli/commands/doctor.ts`, `docs/wiki/specs/cli.md`, `docs/wiki/specs/vault-layout.md` (the spec literals stay in prose but cite the const; the source literals consume the const). Update `INBOX_IS_EPHEMERAL.md` to name the const.
- `f8-build-ai-sdk-tools-deletion` — delete `buildAiSdkTools` from `src/workflows/agent-loop.ts` and the re-export from `src/workflows/index.ts`. Update `tests/workflows/agent-loop.test.ts` to use `filterAiTools(projectAiSdk(vault), names)` directly.
- `f8-bind-ai-sdk-tools-internal` — drop `bindAiSdkTools` from `src/workflows/index.ts` re-exports. Let it stay internal to `src/tools/ai-sdk-binding.ts`. `projectAiSdk` is the canonical public name.
- `f8-workflow-name-tuple` — migrate `src/workflows/workflow-name.ts` from const-object PascalCase shape to the `TOOL_NAMES` tuple shape (`WORKFLOW_NAMES = [...] as const`; `WorkflowName = typeof WORKFLOW_NAMES[number]`). Update call sites — `WorkflowName.Ingest` → `"ingest"` literal.
- `f8-hooks-directory-consolidation` — move `src/hook-dispatcher.ts`, `src/hook-registry.ts`, `src/hook-context.ts` into `src/hooks/`. Update imports across `src/`. Mechanical refactor; no behavior change.
- `f8-bun-glob-substitutions` — replace `walkMd`'s body in `src/vault-fs.ts` with `new Bun.Glob("**/*.md").scan({cwd: root})`. Replace `doctor.ts:walkWikiPages` with `Bun.Glob("wiki/*/*.md")`. Replace `reconcile.ts` inbox walk with `Bun.Glob("inbox/*/*")`.
- `f8-tsconfig-types-bump` — bump `tsconfig.json` `"types": ["bun-types"]` to `["@types/bun"]` (or drop the line entirely — Bun resolves automatically).
- `f8-walk-up-ancestor-extract` — extract `walkUpForAncestor(start: string, accept: (path: string) => boolean): string | null` to `src/path-walk.ts`. `src/vault.ts` `findVaultRoot` and `src/git.ts` `findGitRoot` both consume it.

## Updated substrate counts (post-pass-3)

The substrate uses categorical references rather than inline counts (per `wiki/gotchas/substrate-count-drift.md`). The figures below are the in-flight delta-ledger view, not normative content:

- **Specs:** unchanged — same 8 specs at `docs/wiki/specs/`. Three gain "Adding a new X" recipes; one (page-schema) extends the gotcha frontmatter schema; one (sdk-surface) gains 5 subsection edits; one (prompts-and-workflows) gains a new lifecycle section; one (hooks) gains a recipe section + execution-model edits; one (harnesses) loses a `ConsumerSurface` reference; the eighth (vault-layout) is untouched.
- **Named invariants:** unchanged — same set per `src/types.ts` `INVARIANTS`.
- **Behavior matrices:** unchanged — same set. Two gain lockstep-status preambles.
- **Gotchas:** +2 net. New: `agents-md-delimiter-shape`, `boundary-validation-via-zod`. Eleven existing off-matrix gotchas extend their frontmatter.
- **Semantic linter specs:** +1 net. New: `no-retired-symbol-names`. The existing `wrap-mutating-invoke-consumption` is unchanged.
- **Repo-root orientation files:** +2 net. New: `AGENTS.md`, `CLAUDE.md`.
- **Archival markings:** +1. `docs/cohesive/IMPLEMENTATION_HANDOFF.md` is now banner-archival.

## Deferred (do NOT close in this rewrite)

- **Multi-vault session shape.** F8's multi-vault sub-question is its own brainstorm. The three plausible models (process-per-Vault; multi-vault daemon; vault-pool with LRU eviction) have different consequences for `openVault` lifecycle, queue identity, and on-disk state-file scoping. The composable-construction shape introduced in F3 supports any of the three without committing; the choice happens in a separate `cohesive:brainstorm-design` invocation against the multi-vault scope before any v1 surface starts.
- **Programmatic TS hook loader.** The v0.5.1 deferral named in `hooks.md:37-45` is unchanged by this rewrite. The path partition (`HookSource = "sdk" | "plugin" | "vault-local"`) ships in code; the loader for vault-local `.ts` hooks lands in v0.5.1.
- **HTTP renderer (`renderHttp` + `@dome/sdk/http`).** Named in the consumer-surface matrix as `future-http`; not normative until a v1 HTTP shell ships.
- **Voice renderer (`renderVoice` + `@dome/sdk/voice`).** Same shape as HTTP — named as `future-voice`; not normative until shipped.
- **Cloud sync layer.** Mentioned in VISION; not specced. Out of scope for this rewrite.
- **Parameterized prompts (Zod schema + `getMessages(args)` callback on PromptDescriptor).** Explicitly deferred to v0.5.1+ in the rewritten §"AbstractSurface".

## Repair pass 2

**Source review:** [`docs/cohesive/reviews/2026-05-26-dome-v0.5-to-v1-tightening-rewrite-validation.md`](../reviews/2026-05-26-dome-v0.5-to-v1-tightening-rewrite-validation.md) (pass 1, Issues Found).
**Closes:** B1, B2, B3, I1, I2, I3.

Six small substrate edits, each closing one finding from the pass-1 validation review:

- **B1 closed** — ledger preamble §"Files" line corrected: "5 specs" → "6 specs"; "3 supporting docs" → "2 supporting entries" with the two entries named inline (the `transitive-llm-dependency.md` rewrite + the eleven-off-matrix-gotchas `enforced_at:` frontmatter pass). The substrate-count-drift gotcha in the count-drift ledger itself is closed by aligning the inline tally with the §"Per-file changes" body.
- **B2 closed** — `docs/wiki/linters/no-retired-symbol-names.md` §"Programmatic detection" rewrites the exclusion-set as a bulleted list (instead of inline parenthetical) and explicitly names `docs/wiki/linters/no-retired-symbol-names.md` itself and `docs/index.md` §"Linters" as excluded scan targets. The linter spec is the canonical home of the allow-list; the lockstep skips it rather than maintaining a parallel const elsewhere. Adding a new excluded path is itself substrate (the bulleted list, the test's exclusion list, the test's frontmatter-cross-reference comment must all update together).
- **B3 closed** — `docs/wiki/gotchas/substrate-count-drift.md` `enforced_at:` re-anchors at `tests/integration/no-retired-symbol-names-in-specs.test.ts` (the future test path) with new `enforced_at_status: deferred` frontmatter; `docs/wiki/specs/page-schema.md` §"Extension types" extends the gotcha frontmatter schema to declare the `enforced_at_status: deferred` semantic (the lockstep test warns rather than fails for planned-not-shipped paths until the path lands). Mirrors the existing `coverage: deferred` shape applied to the enforcement-anchor field.
- **I1 closed** — `docs/wiki/specs/sdk-surface.md` §"Distribution" `@dome/sdk` core line names `buildInstructions(vault): Promise<string>` alongside `buildAbstractSurface(vault)`; `docs/wiki/matrices/consumer-surface.md` entrypoint legend `core` line adds `buildInstructions(vault)` to the export catalog. The cold-start instructions composer is now enumerated in the canonical entrypoint and matrix-legend surfaces rather than implicitly assumed by `prompts-and-workflows.md` and `WORKFLOWS_KNOW_VAULT_CONTEXT.md`.
- **I2 closed** — `docs/wiki/gotchas/boundary-validation-via-zod.md` `enforced_at:` re-anchors at `src/hooks/yaml-loader.ts` (the first scar site per the F6 implementation order) rather than `src/tools/schemas.ts` (the precedent the gotcha's mitigation converges on but where the scar does not live).
- **I3 closed** — `docs/wiki/specs/sdk-surface.md` §"Vault" intro pins `openVault`'s return shape as `Promise<Result<Vault, ToolError>>` with one-clause rationale (typed errors at the boundary rather than throws); §"Vault lifecycle" diagram updates `openVault(path) → Vault` to `openVault(path) → Result<Vault, ToolError>   (unwrap to Vault)` so the diagram and the §"Composable construction" code block agree on the signature.

No new substrate concepts were introduced; no design-layer choices were re-derived. Pass-2 is textual repairs against the pass-1 findings.

## History

This rewrite is pass-3-aligned. The earlier passes' delta ledgers are referenced as substrate continuity:

- Pass-1 → Pass-2 transition: `docs/cohesive/delta-ledgers/2026-05-26-dome-v0.5-core-shell-seam.md` shipped the entrypoint split + `ConsumerSurface` introduction.
- Pass-2 → Pass-3 transition: `docs/cohesive/delta-ledgers/2026-05-26-dome-v0.5-second-pass-label-tightening.md` shipped the `AbstractSurface` / `renderMcp` split + `wrapMutatingInvoke` extraction + `HOOK_DISPATCH_IS_VAULT_BOUND` axiom.
- Compiler-reframe: `docs/cohesive/delta-ledgers/2026-05-26-dome-compiler-reframe.md` reframed `dome serve` as compiler daemon + retired `SENSITIVE_GOES_TO_INBOX` + introduced `AGENTS_MD_IS_ORIENTATION_SURFACE` + `VAULT_RECONCILES_AFTER_NATIVE_WRITE` axioms.

This pass-3 rewrite (`dome-v0.5-to-v1-tightening`) is the labels-match-structure pin against the post-pass-2 / post-compiler-reframe substrate. It closes F1-F8 from the pass-3 review at the substrate layer; the implementation pass that follows makes the substrate's claims true in code.
