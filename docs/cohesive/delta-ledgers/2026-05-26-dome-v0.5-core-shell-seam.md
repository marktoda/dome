# Design Delta Ledger — Dome v0.5 core-shell seam (Phase B)

**Date:** 2026-05-26
**Slug:** `dome-v0.5-core-shell-seam`
**Branch:** `design/dome-v0.5-core-shell-seam` (worktree at `.claude/worktrees/design+dome-v0.5-core-shell-seam`, branched off Phase A's `design/dome-v0.5-spec-drift-repair`@`1a3164f`)
**Approved direction source:** Phase B of [`docs/cohesive/reviews/2026-05-26-dome-v0.5-cohesion-architecture-review.md`](../reviews/2026-05-26-dome-v0.5-cohesion-architecture-review.md). User confirmed two load-bearing design choices: (a) move `aiTools` / `toolParsers` off Vault entirely; (b) ship `ConsumerSurface` as a runtime construct.
**Builds on:** [`docs/cohesive/delta-ledgers/2026-05-26-dome-v0.5-spec-drift-repair.md`](2026-05-26-dome-v0.5-spec-drift-repair.md) (Phase A — substrate drift repair).

## Delta at a glance

**Classification:** **Mixed.** Design-layer establishes (a) a new axiom `CORE_HAS_NO_LLM_OR_MCP_DEPENDENCY` pinning the package-bundling boundary; (b) the `ConsumerSurface` runtime construct as the protocol-agnostic four-kind aggregation; (c) the four-entrypoint package shape (`@dome/sdk` core, `@dome/sdk/workflows`, `@dome/sdk/mcp`, `@dome/sdk/cli`); (d) the Vault interface losing `aiTools` and `toolParsers` and gaining `close()`; (e) the Vault lifecycle spec (open → use → drain → close). Three new gotchas document the scars Phase B closes (transitive-llm-dependency, substrate-count-drift, ai-sdk-tool-variance). One new matrix (consumer-surface) names which entrypoint each consumer shell uses. Implementation-layer changes are deferred to `implement-cohesively`: the entrypoint split itself, `vault.close()` implementation, the `projectAiSdk` / `projectMcp` / `buildConsumerSurface` functions, four new lockstep tests (bundle-deps + gotcha-coverage + matrix-coverage + consumer-surface), and the call-site updates for every consumer of `vault.aiTools` and `vault.toolParsers`.

**Files:** 7 substrate files added (1 invariant, 3 gotchas, 1 matrix, 1 new delta ledger, 1 new test file is implementation-deferred); 5 substrate files rewritten (4 specs, 1 matrix); 1 substrate catalog updated (`docs/index.md`).

**Conceptual changes:** The **four-entrypoint package shape** is the structural realization of the package-bundling boundary the four-concept core's seal was missing. **`ConsumerSurface`** names the four-kind aggregation pattern every v1+ protocol adapter consumes. **Vault sheds aiTools/toolParsers**; the projections live in entrypoint-scoped functions consumers reach explicitly. **Vault lifecycle** is now first-class spec content (open/use/drain/close).

**Named invariants:** `CORE_HAS_NO_LLM_OR_MCP_DEPENDENCY` (axiom, added). Total invariant tier: 7 axiom + 4 shipped-default + 2 opt-in = 13.

**Behavior matrices:** `consumer-surface.md` (added). `tool-invariant-enforcement.md` updated to add the new axiom's off-matrix section.

**Gotchas:** `transitive-llm-dependency.md`, `substrate-count-drift.md`, `ai-sdk-tool-variance.md` (3 added).

**Semantic linters:** `bundle-deps.test.ts` proposed as the structural enforcement for `CORE_HAS_NO_LLM_OR_MCP_DEPENDENCY`; `dome doctor --check-substrate-counts` proposed as the mitigation for `substrate-count-drift` (deferred to v0.5.1+ per the gotcha doc).

**Tests proposed (implementation lands in implement-cohesively):**

- `tests/integration/bundle-deps.test.ts` — pins `CORE_HAS_NO_LLM_OR_MCP_DEPENDENCY`.
- `tests/integration/gotcha-coverage.test.ts` — AC3-shape lockstep test: every `docs/wiki/gotchas/*.md` has a corresponding `tests/gotchas/<slug>.test.ts` OR is explicitly marked off-matrix in the gotcha frontmatter.
- `tests/integration/matrix-coverage.test.ts` — AC3-shape lockstep test: every bolded enforcement cell in `tool-invariant-enforcement.md` maps to a test name pattern in `tests/invariants/<INVARIANT>.test.ts`.
- `tests/integration/consumer-surface.test.ts` — exercises `buildConsumerSurface(vault)` end-to-end; asserts the returned shape and that `DomeMcpServer({ surface })` exposes the same surface as the v0.5 direct-Vault construction.

**Deferred (post-Phase-B):**

- The v1+ surfaces themselves (mobile, desktop, voice, web) — Phase B ships only the contract.
- `dome doctor --check-substrate-counts` semantic linter — gotcha doc names it; implementation deferred to v0.5.1+ when substrate growth makes manual link-checking expensive.
- The `parseLogEntries` consolidation from the structure review's Finding 2 — listed as a candidate follow-up; not in Phase B scope.
- Post-close-Vault guard (calling tools after `vault.close()` is undefined behavior in v0.5; future versions may enforce).
- Per-bucket `inbox.buckets.<name>.ephemeral` knob — deferred per `INBOX_IS_EPHEMERAL.md` §"Per-bucket disable" from Phase A.

## Approved direction (verbatim from architecture review Phase B + user confirmation)

Phase B closes the v1-extensibility seam findings F1 (core/shell boundary half-built), F2 (no name for "MCP-style aggregation"), and F6 (no lockstep test for matrices or gotchas). The user confirmed at dispatch: move `aiTools` / `toolParsers` off Vault entirely (not lazy-getter); ship `ConsumerSurface` as a runtime construct (not just a documented pattern).

## Per-file changes

### Substrate added (7 files)

#### `docs/wiki/invariants/CORE_HAS_NO_LLM_OR_MCP_DEPENDENCY.md` — new axiom

Stable ID for implement-cohesively: `core-llm-mcp-axiom`.

- **Statement:** `@dome/sdk` core does not transitively depend on `@anthropic-ai/sdk`, `ai`, or `@modelcontextprotocol/sdk`.
- **Structural enforcement:** `tests/integration/bundle-deps.test.ts` walks the dependency graph from `src/index.ts` and asserts the three forbidden packages are absent.
- **Off-matrix tier:** the matrix at `tool-invariant-enforcement.md` does not gain a column — no Tool refuses an invariant-violating call. Bundle-time enforcement.
- **Implementation work this entry implies:** add `CORE_HAS_NO_LLM_OR_MCP_DEPENDENCY` to `INVARIANTS` const in `src/types.ts`; create `tests/integration/bundle-deps.test.ts`; the AC3 invariant-coverage test will then auto-require `tests/invariants/core-has-no-llm-or-mcp-dependency.test.ts` to exist (or accept the bundle-deps test as the off-matrix equivalent — implementer's choice, document in the test).

#### `docs/wiki/gotchas/transitive-llm-dependency.md` — new gotcha (High severity)

Stable ID: `transitive-llm-dep-gotcha`.

Documents the symptom Phase B closes: pre-Phase-B, `openVault` transitively pulled Anthropic + MCP into every consumer's bundle. Mitigation: the new axiom plus the entrypoint split. No code work this gotcha implies beyond the rest of Phase B.

#### `docs/wiki/gotchas/substrate-count-drift.md` — new gotcha (Medium severity)

Stable ID: `count-drift-gotcha`.

Documents the link-to-canonical-surface convention Phase A's spec rewrite established. Mitigation: the convention is now substrate; `dome doctor --check-substrate-counts` is the deferred mechanical enforcement.

#### `docs/wiki/gotchas/ai-sdk-tool-variance.md` — new gotcha (Low severity)

Stable ID: `ai-sdk-variance-gotcha`.

Documents the deliberate scar at `src/tools/registry.ts:128` (the `as unknown as Tool<TInput, TOutput>` cast) and `:215` (`satisfies Record<ToolName, ToolRegistryEntry<any, any>>`). No code work this gotcha implies; it pins the rationale for the next AI SDK major bump.

#### `docs/wiki/matrices/consumer-surface.md` — new matrix

Stable ID: `consumer-surface-matrix`.

Rows: 9 consumer shells (CLI, MCP server, headless agent loop, future mobile, future desktop, future voice, future HTTP, plugin SDK, eval suite). Columns: 12 exported symbol families. Cells: which entrypoint (`core` / `workflows` / `mcp` / `cli` / `—`) the consumer reaches the symbol through.

#### `docs/cohesive/delta-ledgers/2026-05-26-dome-v0.5-core-shell-seam.md` — this file (new)

Stable ID: `phase-b-ledger`.

The audit trail for Phase B.

### Substrate rewritten (5 files)

#### `docs/wiki/specs/sdk-surface.md` — rewrite (4 sections changed; 2 new sections)

Stable IDs:
- `sdk-vault-surface-rewrite` — §"Vault surface" loses `aiTools` and `toolParsers` from the list; the doc now names `projectAiSdk(vault)` and `projectMcp(vault)` as entrypoint-scoped functions. Adds `close(): Promise<void>` to the Vault surface list.
- `sdk-consumer-surfaces-section` — new §"Consumer surfaces" section names the `ConsumerSurface` type + `buildConsumerSurface(vault)` factory + the four-kinds-in-fixed-order convention. `DomeMcpServer` is described as a thin protocol adapter over it.
- `sdk-vault-lifecycle-section` — new §"Vault lifecycle" section spells out open → use → drain → close with `drainHooks` idempotency and `close()` one-shot semantics. Watcher lifecycle is caller-owned, outside the Vault.
- `sdk-distribution-rewrite` — §"Runtime" §"Distribution" rewritten: two entrypoints → four entrypoints (`@dome/sdk`, `@dome/sdk/workflows`, `@dome/sdk/mcp`, `@dome/sdk/cli`). Per-entrypoint dep list.
- `sdk-dependencies-rewrite` — §"Dependencies" table gets an "Entrypoint scope" column. `remark` / `unified` dropped (never imported — see review §F7).

#### `docs/wiki/specs/mcp-surface.md` — rewrite

Stable ID: `mcp-surface-rewrite`.

Introductory paragraph rewritten: MCP server is now a "thin protocol adapter over `ConsumerSurface`." Adds a §"Construction" subsection showing `buildConsumerSurface(vault)` → `new DomeMcpServer({ surface })`. The `@dome/sdk/mcp` entrypoint is named as the canonical home.

#### `docs/wiki/specs/harnesses.md` — rewrite (§"Future-harness pressure")

Stable ID: `harnesses-future-pressure-rewrite`.

The four future-harness bullets are rewritten to name which entrypoint each consumes (mobile = `core`, voice = `core` for capture / `workflows` for ingest, web = `core` + future `@dome/sdk/http`). New normative line: "future harness shells construct against `ConsumerSurface`." Reference to `consumer-surface.md` matrix.

#### `docs/wiki/matrices/tool-invariant-enforcement.md` — rewrite

Stable ID: `tool-invariant-matrix-update`.

Adds `CORE_HAS_NO_LLM_OR_MCP_DEPENDENCY` to the "Not in the matrix because..." list with off-matrix justification. New `### CORE_HAS_NO_LLM_OR_MCP_DEPENDENCY — bundle-enforced (off-matrix)` section names `tests/integration/bundle-deps.test.ts` as the structural fence.

#### `docs/index.md` — update

Stable ID: `index-catalog-update`.

Adds `CORE_HAS_NO_LLM_OR_MCP_DEPENDENCY` to §"Invariants"; adds `consumer-surface` to §"Matrices"; adds `transitive-llm-dependency`, `substrate-count-drift`, `ai-sdk-tool-variance` to §"Gotchas". Replaces the inline "Six axioms" line with a link-to-canonical pointer (`src/types.ts` `INVARIANTS`) per the substrate-count-drift convention.

## Implementation work (deferred to implement-cohesively)

The substrate rewrite above describes an end state where the following code changes are true. The full delta-entry list for `implement-cohesively` Step 1:

**Substrate-pinning code changes:**

1. `code-invariants-const` — add `CORE_HAS_NO_LLM_OR_MCP_DEPENDENCY: "CORE_HAS_NO_LLM_OR_MCP_DEPENDENCY"` to `INVARIANTS` const in `src/types.ts`.
2. `code-bundle-deps-test` — create `tests/integration/bundle-deps.test.ts` asserting the core entrypoint excludes `@anthropic-ai/sdk`, `ai`, `@modelcontextprotocol/sdk`.

**Vault surface refactor:**

3. `code-vault-interface-shrink` — remove `aiTools: ai.ToolSet` and `toolParsers` from the `Vault` interface in `src/vault.ts`. Add `close(): Promise<void>`.
4. `code-vault-close-impl` — implement `vault.close()` as `await drainHooks()` + any held-resource release.
5. `code-vault-openVault-impl` — `openVault` stops constructing `aiTools` / `toolParsers`. The `bindTools()` helper at `src/tools/registry.ts` no longer needs the AI SDK or zod-to-json-schema imports at the boundary.

**Entrypoint split:**

6. `code-entrypoint-package-json` — add `./workflows` and `./mcp` entries to `package.json` `exports`.
7. `code-workflows-entrypoint` — create `src/workflows/index.ts` exporting `runWorkflow`, `WorkflowRegistry`, `PromptLoader`, `projectAiSdk`, `DEFAULT_MODEL`, `DEFAULT_MAX_STEPS`, types.
8. `code-mcp-entrypoint` — create `src/mcp/index.ts` exporting `DomeMcpServer`, `buildConsumerSurface`, `ConsumerSurface` type, `projectMcp`, the adapter constructors.
9. `code-core-index-prune` — remove from `src/index.ts` (core): `runWorkflow`, `buildAiSdkTools`, `DEFAULT_MODEL`, `DEFAULT_MAX_STEPS`, `WorkflowRegistry`, `PromptLoader`, `parseWorkflowFrontmatter`, `isWorkflowPrompt`, `WorkflowFrontmatterSchema`, `makeFixtureVault`, `DomeMcpServer`, `buildToolAdapters`, `buildPromptAdapters`, `ResourceAdapter`, `ResourceUri`, `McpToolName`.

**ConsumerSurface:**

10. `code-consumer-surface-impl` — implement `ConsumerSurface` type and `buildConsumerSurface(vault)` factory in `src/mcp/consumer-surface.ts` (or `src/mcp/index.ts`); refactor `DomeMcpServer` to accept `{ surface }` instead of `{ vault }`.

**Tool projections:**

11. `code-project-ai-sdk` — `projectAiSdk(vault): ai.ToolSet` in `src/workflows/`; replaces the inlined `vault.aiTools` reads in `runWorkflow`.
12. `code-project-mcp` — `projectMcp(vault)` in `src/mcp/`; replaces the inlined `vault.toolParsers` reads in `buildToolAdapters`.

**Call-site updates:**

13. `code-cli-imports` — `src/cli/run-workflow-at-path.ts` and the four LLM-driven CLI commands (`lint`, `migrate`, `export-context`, `migrate` again) import `runWorkflow` from `@dome/sdk/workflows` (or relative path equivalent).
14. `code-serve-imports` — `src/cli/commands/serve.ts` imports `DomeMcpServer` + `buildConsumerSurface` from `@dome/sdk/mcp`.

**Test additions:**

15. `code-gotcha-coverage-test` — `tests/integration/gotcha-coverage.test.ts`: every `docs/wiki/gotchas/*.md` has a corresponding `tests/gotchas/<slug>.test.ts` OR carries `coverage: off-matrix` in its frontmatter (allowing the count-drift / variance / transitive-dep gotchas to be marked correctly).
16. `code-matrix-coverage-test` — `tests/integration/matrix-coverage.test.ts`: every bolded cell in `tool-invariant-enforcement.md` maps to a test name pattern.
17. `code-consumer-surface-test` — `tests/integration/consumer-surface.test.ts`: exercises `buildConsumerSurface(vault)` end-to-end; asserts the four-kind shape and that the resulting `DomeMcpServer` exposes equivalent surface to the pre-refactor direct-Vault construction.

**Dependency cleanup:**

18. `code-drop-remark` — remove `remark` and `unified` from `package.json` dependencies (never imported).

**Total delta entries for implement-cohesively: 18.** Above the 15-entry budget gate threshold. `implement-cohesively` Step 1 will surface this; user may split into B1 (substrate-pinning + Vault refactor + entrypoint split — ~10 entries) and B2 (lockstep tests + cleanup — ~8 entries) at that point.

## Positions preserved verbatim (no change this pass)

To document what Phase B deliberately doesn't touch:

- The four-concept core (Vault, Document, Tool, Hook) and the seven-Tool registry. Phase B reaffirms the seal at the bundling layer; it doesn't add a fifth concept.
- All twelve pre-Phase-B invariants (RAW_IS_IMMUTABLE through PAGE_CREATION_REQUIRES_RECURRENCE). `CORE_HAS_NO_LLM_OR_MCP_DEPENDENCY` is the 13th; the others are untouched.
- All Phase A invariants and substrate (INBOX_IS_EPHEMERAL's two-layer enforcement, quarantine persistence, doctor flag implementations). Phase B builds on Phase A's branch.
- The CLI command surface — 7 commands, unchanged. `dome init`'s AGENTS.md + CLAUDE.md shim shape from Phase A is preserved.
- The MCP tool/prompt/resource catalog content — the seven tool names, the workflow set, the three resource URIs. Phase B changes how these are constructed (via `ConsumerSurface`) but not what they are.

## Acceptance criteria check

- ✓ All affected docs in end-state language; no "we will" / "should consider" in normative sections. The future-harness section in `harnesses.md` is explicitly non-normative per its header.
- ✓ Obsolete concepts (`Vault.aiTools`, `Vault.toolParsers`) removed from the spec, not annotated.
- ✓ Design delta ledger at the canonical path.
- ✓ `## Delta at a glance` preamble filled in after body stabilized.
- ✓ Substrate map (`docs/index.md`) updated for the three new gotchas + the new invariant + the new matrix.
- ✓ Rewrite happens on `design/dome-v0.5-core-shell-seam` branch in a worktree.
- ⏳ Atomic commit next.
- ✓ Skill does not invoke `validate-rewrite` — handoff is announced.
