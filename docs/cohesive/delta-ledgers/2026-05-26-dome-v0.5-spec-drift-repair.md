# Design Delta Ledger — Dome v0.5 spec drift repair (Phase A)

**Date:** 2026-05-26
**Slug:** `dome-v0.5-spec-drift-repair`
**Branch:** `design/dome-v0.5-spec-drift-repair` (worktree at `.claude/worktrees/design+dome-v0.5-spec-drift-repair`)
**Approved direction source:** [`docs/cohesive/reviews/2026-05-26-dome-v0.5-cohesion-architecture-review.md`](../reviews/2026-05-26-dome-v0.5-cohesion-architecture-review.md), Phase A — with the user's course-correction at dispatch time: instead of partitioning `dome doctor` flags into "shipped vs deferred" in the spec, **implement the deferred flags so the spec stays honest**. Code catches up to the spec rather than the spec retreating to the code.
**Substrate discovery:** [`docs/cohesive/substrate-discovery/2026-05-26-dome-v0.5-self-review.md`](../substrate-discovery/2026-05-26-dome-v0.5-self-review.md)

## Delta at a glance

**Classification:** **Mixed.** Design-layer changes describe an end state where (a) `INBOX_IS_EPHEMERAL` has a structural doctor-check fallback alongside the workflow-prompt enforcement, (b) `.dome/state/quarantined.json` joins `scheduled.json` and `last-reconciled-sha.txt` as canonical derived state, (c) inline counts in synthesis docs link to canonical const-array surfaces instead of inlining stale numbers, and (d) `dome init`'s shipped scaffolding (`AGENTS.md` + `CLAUDE.md` shim) is normatively documented in the CLI spec rather than implied by source code. Implementation-layer changes are deferred to `implement-cohesively`: six `dome doctor` flags currently no-op silently and the new `INBOX_IS_EPHEMERAL` doctor walk, the quarantine persistence wiring, and the extended `SHIPPED_AGENTS_MD` cold-start fallback.

**Files:** 6 substrate files rewritten (4 specs, 1 invariant, 1 prior delta ledger updated with a current-state column). 0 substrate files added (the substrate-count-drift gotcha is deferred to Phase B). 0 substrate files removed.

**Conceptual changes:** the **derived-state-table-as-single-source** convention is established (the `sdk-surface.md` derived-state subsection now links to `vault-layout.md` rather than duplicating the table). The **link-to-canonical-surface convention** for inline counts is established (the `v0.5-build-plan.md` workflow + CLI-command counts now reference their canonical specs). The **two-layer enforcement pattern** is formalized for `INBOX_IS_EPHEMERAL` (workflow-prompt primary + doctor-check fallback) — this is the first explicit articulation of a pattern that other prompt-enforced invariants may want to follow in v1+.

**Named invariants:** no new invariants. `INBOX_IS_EPHEMERAL` updated in place to add the structural-fallback documentation.

**Behavior matrices:** no new matrices. The `tool-invariant-enforcement.md` §"`INBOX_IS_EPHEMERAL` — workflow-enforced (off-matrix)" cross-reference points at the now-strengthened invariant doc.

**Gotchas:** no new gotchas this pass. The `substrate-count-drift.md` gotcha that the architecture review surfaced is deferred to Phase B (under slug `dome-v0.5-core-shell-seam`); this pass repairs the live drifts but doesn't yet pin the rule structurally.

**Semantic linters:** none added; substrate-count-drift detection lives in the deferred Phase B work.

**Tests proposed:** `tests/invariants/inbox-is-ephemeral.test.ts` extended in implement-cohesively to cover the doctor-fallback layer (backdated fixture file + assertion that doctor emits the violation; `inbox/review/` exclusion check). No new test files.

**Deferred (Phase B):** new axiom `CORE_HAS_NO_LLM_OR_MCP_DEPENDENCY`, new matrix `consumer-surface.md`, new gotchas `transitive-llm-dependency.md` and `substrate-count-drift.md` and `ai-sdk-tool-variance.md`, new spec sections `sdk-surface.md` §"Consumer surfaces" and §"Vault lifecycle", new entrypoints `@dome/sdk/workflows` and `@dome/sdk/mcp`, lockstep tests for matrices and gotchas, the entrypoint split itself.

## Per-file changes

### `docs/wiki/specs/cli.md` — rewrite

Closes architecture review F3 (doctor-flag spec drift), F7 (small concept collapse — AGENTS.md shape now spec'd), F8 (shipped AGENTS.md description in the spec).

1. **§"`dome init`" `Creates:` list** — replaced the single-line `CLAUDE.md` template entry with two entries: (a) `AGENTS.md` as the vault-owned cross-harness convention file with cold-start orientation including the offline-rule-surface pointer to `docs/wiki/invariants/`, and (b) `CLAUDE.md` as a thin shim pointing at `AGENTS.md`. The shipped scaffolding shape is now normatively specified rather than implied by `src/cli/commands/init.ts`.
2. **§"`dome doctor`" structural-violations list** — added an "Inbox files older than `hooks.inbox_stale_age_hours`" bullet citing the `INBOX_IS_EPHEMERAL` invariant and explicitly excluding `inbox/review/` (because `review/` is the `SENSITIVE_GOES_TO_INBOX` destination, not an intake).
3. **§"`dome doctor` Flags" `--reset-quarantined-hooks`** — the flag description now names `.dome/state/quarantined.json` as the persistent quarantine record and cross-references the derived-state table in `vault-layout.md`. The justification — that `dome doctor` and `dome serve` don't share a process — is named so a future contributor sees why the file exists.

### `docs/wiki/invariants/INBOX_IS_EPHEMERAL.md` — rewrite

Closes F3 (invariant doctor-check claim aligned with end-state implementation).

1. **§"Structural enforcement"** — restructured from one paragraph to two numbered layers: (1) workflow-prompt instruction (primary, prompt-enforced); (2) `dome doctor` structural fallback (secondary, defends against prompt regression). The doctor-check claim is now concrete: walks `inbox/<bucket>/` excluding `review/`, emits a violation per file older than `hooks.inbox_stale_age_hours` (default 24h).
2. **§"Test guarantee"** — restructured from one sentence to two numbered layers matching the enforcement layers: workflow-prompt-binding assertions (existing) and doctor-fallback assertions (new — backdated fixture + violation assertion + `review/` exclusion check).
3. **Stale "24 hours" inline literal** — replaced with a reference to the doctor check that now lives in §"Structural enforcement"; the configurable threshold (not a hard-coded 24h) is the contract.

### `docs/wiki/specs/vault-layout.md` — rewrite

Closes F3 (derived-state inventory updated for new quarantine state).

1. **§"Derived operational state under `.dome/`" table** — added the `.dome/state/quarantined.json` row (role: hook quarantine list; if deleted: quarantined handlers re-enter rotation, idempotent so safe). The framing paragraph below the table now says "all three files" instead of "both files."

### `docs/wiki/specs/hooks.md` — rewrite

Closes F3 (failure-model section makes quarantine persistence explicit; new config-fields table consolidates `hooks.*` documentation).

1. **§"Execution model" Failure model** — rewrote to make persistence explicit: `quarantined.json` is named as the cross-process quarantine record. Removed the implicit "until `dome doctor` is run" phrasing in favor of the explicit `dome doctor --reset-quarantined-hooks` reference. The "necessary because doctor and serve don't share a process" justification is named.
2. **§"`.dome/config.yaml` hook-related fields" (new subsection)** — added a small table listing the three `hooks.*` config fields: `hooks.builtin.<id>`, `hooks.max_causation_depth` (preserved from §"Cycle prevention" — that paragraph still names it inline), and `hooks.inbox_stale_age_hours` (new, default 24, links to `INBOX_IS_EPHEMERAL`). This is the canonical home for the `hooks.*` config-shape spec; `sdk-surface.md` §"Tiered feature model" still points at `src/shipped-defaults.ts` as the live-value source of truth.

### `docs/wiki/specs/sdk-surface.md` — rewrite

Closes F3 (collapses the duplicated derived-state inventory; small concept collapse).

1. **§"Derived operational state on disk"** — replaced the inline two-file table with a one-paragraph cross-reference to `vault-layout.md` §"Derived operational state under `.dome/`" as the canonical inventory. The justification (deleting the files doesn't lose canonical knowledge) is preserved; the file list is no longer duplicated. This implements the **derived-state-table-as-single-source** convention this pass establishes.

### `docs/wiki/syntheses/v0.5-build-plan.md` — rewrite

Closes F4 (substrate-count drift in the synthesis).

1. **§"v0.5 — Demo" Ships list** — two count drifts repaired by the link-to-canonical-surface convention rather than by inline-count update:
   - "Prompt library: ... (8 workflow prompts) + system-base.md" → "Prompt library: the shipped workflow prompts (canonical list: [[wiki/specs/prompts-and-workflows]] §'Shipped workflows by tier') + `system-base.md`."
   - "CLI: 5 commands (init, migrate, serve, lint, doctor, export-context)." → "CLI: the shipped command surface (canonical list: [[wiki/specs/cli]] §'Implementation note')."

   Excising inline counts in favor of links to the canonical specs is the structural prevention of future drift; the substrate-count-drift gotcha to be created in Phase B will name this as a substrate-wide convention.

### `docs/cohesive/delta-ledgers/2026-05-25-dome-v0.5-foundation.md` — update (audit-trail repair)

Closes F4 (prior delta ledger count drift).

1. **§"Updated substrate counts" title** — "after all three passes" → "after all passes including pass 5" (the prior title was stale by two further repair passes).
2. **Table** — added a new "Final (post pass-5)" rightmost column reflecting the live state of `main`:
   - Invariants: **12 — split 6 axiom + 4 default + 2 opt-in** (added INDEX_AND_LOG_ARE_DISPATCHER_OWNED as 6th axiom in pass 5).
   - SDK Tools: **7** (deleteDocument was added between the original brainstorm collapse and the current state).
   - Total substrate docs: **46** (12 invariants × 1 doc each + the other categories per row).
   - Hard dependencies: + **commander** (CLI v14 migration since the durability pass).
   - Axiom invariants: **6** (added INDEX_AND_LOG_ARE_DISPATCHER_OWNED).
   - Shipped-default invariants: new row, value **4** (the prior table didn't break out shipped-default count separately from total).
   - Opt-in invariants: new row, value 2.
3. **Canonical-inventories footer paragraph** — added a short footer naming the canonical inventories that future readers should treat as ground truth (link instead of inlining counts): `docs/wiki/invariants/`, the CLI implementation-note table, the workflows tier table, and the typed const arrays in `src/types.ts` and `src/tools/registry.ts`. This is the inline-foreshadowing of the substrate-count-drift gotcha to come in Phase B.

## Positions preserved verbatim (no change this pass)

To document the surfaces this pass deliberately doesn't touch (so the next reviewer sees the scope):

- The four-concept core (`sdk-surface.md` §"The four concepts") and the seven-Tool registry — sealed, not in scope.
- `RAW_IS_IMMUTABLE`, `MARKDOWN_IS_SOURCE_OF_TRUTH`, `LOG_IS_APPEND_ONLY`, `HOOKS_CANNOT_BYPASS_TOOLS`, `VAULT_IS_GIT_REPO`, `INDEX_AND_LOG_ARE_DISPATCHER_OWNED`, `EVERY_WRITE_IS_LOGGED`, `PAGE_TYPE_BY_DIRECTORY`, `WIKILINKS_ARE_FULLPATH`, `SENSITIVE_GOES_TO_INBOX`, `PAGE_CREATION_REQUIRES_RECURRENCE` — untouched. Only `INBOX_IS_EPHEMERAL` is rewritten this pass.
- All eight gotcha docs — untouched.
- All three behavior matrices — untouched. The matrix cell that names `INBOX_IS_EPHEMERAL` as off-matrix at `tool-invariant-enforcement.md` §"`INBOX_IS_EPHEMERAL` — workflow-enforced (off-matrix)" still applies; the doctor-check is "off-matrix structural fallback," not Tool-boundary enforcement.

## Implementation handoff (forward reference)

The substrate now describes an end state where:

- `dome doctor` implements all 9 documented flags (3 already functional: `--rebuild-index`, `--show workflows`, `--show events`; 6 currently no-op: `--drain-hooks`, `--recent-activity [N]`, `--reset-quarantined-hooks`, `--show review-queue`, `--show raw-citations`, `--show recent-hook-cycles`).
- `dome doctor` also implements the new `INBOX_IS_EPHEMERAL` structural check (inbox-age walk, configurable threshold, `review/` exclusion).
- `HookRegistry` persists its quarantine state to `.dome/state/quarantined.json` rather than holding it in-memory only.
- `SHIPPED_VAULT_CONFIG` in `src/shipped-defaults.ts` includes `hooks.inbox_stale_age_hours: 24`.
- `SHIPPED_AGENTS_MD` in `src/cli/commands/init.ts` includes the offline-rule-surface pointer to `docs/wiki/invariants/`.
- `tests/invariants/inbox-is-ephemeral.test.ts` covers both enforcement layers (existing prompt-binding test + new doctor-fallback test).

The implementation pass — `cohesive:implement-cohesively` against this delta ledger — should produce one logical change per delta-ledger entry, with the per-workflow-atomic-commit policy producing one git commit per substantive unit of work (or one commit per delta-entry, at the implementer's discretion).

## Acceptance criteria check

Against the `rewrite-specs` skill's acceptance criteria:

- ✓ All affected docs are in end-state language (present-tense "the system does X"; no "we will" / "should consider" in normative sections). The few "deferred to Phase B" mentions live in the §"Deferred" and §"Implementation handoff" sections, which are explicitly non-normative meta-commentary.
- ✓ Obsolete concepts removed, not annotated. The stale "CLAUDE.md template" bullet was *replaced* by the AGENTS.md + CLAUDE.md-shim bullets, not annotated.
- ✓ Design delta ledger exists at the canonical path `docs/cohesive/delta-ledgers/2026-05-26-dome-v0.5-spec-drift-repair.md`.
- ✓ `## Delta at a glance` preamble filled in after the body stabilized.
- ✓ Substrate map (`docs/index.md`) does not need update — no specs added/removed; the invariant inventory and the spec catalog are unchanged.
- ✓ Rewrite happens on `design/dome-v0.5-spec-drift-repair` branch in a worktree.
- ✓ Atomic commit follows (next step).
- ✓ Skill does not invoke `validate-rewrite` — handoff is announced; user invokes the review.
