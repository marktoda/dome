{
  "name": "@dome/ai-processor",
  "version": "0.1.0",
  "description": "AI content processing service for Dome",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "@cloudflare/workers-types": "^4.20240320.1",
    "@dome/common": "workspace:*",
    "@dome/logging": "workspace:*",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vitest": "^1.2.1",
    "wrangler": "^3.28.1"
  }
}
