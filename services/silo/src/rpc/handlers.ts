/**
 * RPC Handlers stub for Silo service.
 * Actual implementations will be added in later stages.
 */

export const rpcHandlers = {
  // Example:
  // simplePut: async (data: any, env: any) => { /* ... */ },
};