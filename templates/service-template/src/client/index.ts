// Export the client and types for easy importing
export { {{SERVICE_NAME}}Client } from './client';
export type {
  {{SERVICE_NAME}}ClientOptions,
  {{SERVICE_NAME}}Response,
  {{SERVICE_NAME}}Request,
  HealthCheckResponse,
} from './types';