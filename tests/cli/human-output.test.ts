// human-output.ts retains only formatting utilities (plural, formatSeverity,
// formatCommand, truncateText). The layout helpers (formatHeadline,
// pushSection, formatSummaryRows) were migrated to the presenter module and
// their tests removed.
