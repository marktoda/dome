id: test.scheduled-slow
version: 0.1.0
processors:
  - id: test.scheduled-slow.wait-for-abort
    version: 0.1.0
    phase: garden
    triggers:
      - kind: schedule
        cron: "* * * * *"
    capabilities: []
    execution:
      class: background
      timeoutMs: 600000
    module: processors/wait-for-abort.ts
